# QA Sweep — 2026-03-30

Comprehensive test suite audit and bug-fixing session. Goal: find every little thing wrong in JVE by making the test suite as adept as possible at ferreting out issues.

## Commits

| Hash | Description |
|------|-------------|
| `728df50` | QA sweep: 11 bugs fixed, 13 tests added, 8 dead tests removed |
| `6c197ba` | Mutation system preserves clip volume/marks/playhead through undo |
| `00b9cdf` | Inspector mark_out display off-by-one + overlap invariant test |
| `f3e6a55` | frame_utils fps=0 asserts instead of silent fallback to 1 |
| `081adb6` | Delete test_resolve_database_import (exploratory doc, no assertions) |
| `5da6053` | Delete 3 dead code files (modify_property, set_property, debounce) |
| `bac739b` | Eliminate duplicate parse_timecode — timecode.lua delegates to frame_utils |
| `0bf8067` | SplitClip preserves volume on second half |
| `68a1f72` | Duplicate clips preserve volume |
| `7817934` | DeleteMasterClip undo preserves volume + timestamps on timeline clips |
| `b6ed566` | RippleDeleteSelection captures/restores clip properties on undo |
| `e6687d8` | load_track_clips includes volume in SELECT (resolve_occlusions path) |
| `df9d1ca` | QA sweep documentation |
| `d040ef4` | Overwrite undo volume loss test + documentation |
| `ff20507` | Import undo (DRP + FCP7) cleans up properties/links before clip delete |
| `d4b1193` | ImportMedia undo cleans up properties/links before clip delete |
| `2658bef` | Clip:delete() automatically cleans up properties + clip_links (systemic) |
| `251817a` | Media:delete() cleans up clip properties/links before bulk delete |
| `8398e8e` | Test: Clip:delete() orphan cleanup (14 checks) |
| `6ce4af4` | UpdateSmartBin undo restores NULL scope_bin_id |
| `7afc040` | resolve_occlusions straddle split preserves volume + master_clip_id |
| `fa4abfe` | All 3 clip_mutator split paths preserve volume + ownership |

## Bugs Fixed (29)

### Crash on invoke (3)
| File | Bug |
|------|-----|
| `mark_clip_extent.lua` | Missing `project_id` when delegating to SetMarkIn/SetMarkOut |
| `delete_selection.lua` | Missing `project_id` in RippleDeleteSelection delegation |
| `delete_selection.lua` | Missing `project_id` in RippleDelete gap delegation |

### Undo data loss (10)
| File | Bug |
|------|-----|
| `delete_sequence.lua` | Undo lost 4 sequence fields (start_timecode, scroll offsets, split ratio) |
| `delete_sequence.lua` | Undo lost 4 clip fields (volume, marks, playhead) |
| `delete_sequence.lua` | Undo lost all clip_links (A/V sync) — referenced non-existent columns |
| `delete_sequence.lua` | Undo lost all clip properties — fetch function existed but was never called |
| `command_helper.lua` | `capture_clip_state` missing volume/marks/playhead (systemic: affects all delete commands) |
| `command_helper.lua` | `apply_mutations` INSERT missing volume/marks/playhead columns (systemic: affects all insert/redo) |
| `command_helper.lua` | `revert_mutations` undo-delete INSERT missing same 4 columns |
| `split_clip.lua` | Second half of split loses volume (gets default 1.0) |
| `clip_mutator.lua` | `load_track_clips` missing volume — resolve_occlusions stores nil volume in previous state |
| `ripple_delete_selection.lua` | Properties not captured before deletion, lost on undo |

### Volume loss in clip creation (3)
| File | Bug |
|------|-----|
| `clip_mutator.lua` | `plan_insert` didn't capture volume/marks/playhead from source clip |
| `clip_mutator.lua` | `load_clip_for_duplicate_plan` SELECT missing volume |
| `clip_mutator.lua` | `resolve_occlusions` straddle split: right half loses volume + master_clip_id + owner_sequence_id |
| `clip_mutator.lua` | `resolve_occlusions_multi` fragment split: missing volume + owner_sequence_id |
| `clip_mutator.lua` | `resolve_ripple` straddle split: missing volume |
| `delete_master_clip.lua` | Timeline clip snapshot missing volume/timestamps, undo Clip.create didn't pass volume |

### Silent data corruption (2)
| File | Bug |
|------|-----|
| `schema.sql` | `clips.project_id` nullable — allowed orphaned clips; load_clips silently healed with fallback |
| `sequence.lua` | `created_at` overwritten with `os.time()` on load; `selected_gap_infos` not loaded/saved |

### Display bug (1)
| File | Bug |
|------|-----|
| `inspector/view.lua` | mark_out displayed as exclusive boundary (1 frame too far for user) |

### Orphaned data on clip deletion (4)
| File | Bug |
|------|-----|
| `import_resolve_project.lua` | Undo deleted clips without cleaning properties/links (+ migrated to parameterized SQL) |
| `import_fcp7_xml.lua` | Same orphan pattern on undo |
| `import_media.lua` | Same orphan pattern on undo |
| `clip.lua` (Clip:delete) | Model-level delete didn't clean properties/links — systemic fix, all callers now covered |
| `media.lua` (Media:delete) | Bulk DELETE clips WHERE media_id bypassed Clip:delete() — orphaned properties/links |

### Undo logic bugs (1)
| File | Bug |
|------|-----|
| `smart_bin_commands.lua` | UpdateSmartBin undo didn't restore NULL scope_bin_id (project-wide → scoped, undo left scoped) |

## Code Quality Fixes (3)
| File | Fix |
|------|-----|
| `frame_utils.lua` | fps=0 silent fallback to 1 replaced with assert |
| `timecode.lua` | Duplicate `parse_timecode` (35 lines) replaced with delegation to frame_utils |
| `database.lua` | Dead `update_clip_position()` removed (wrong column names, never called) |

## Dead Code Removed (5 files, ~500 lines)
| File | Reason |
|------|--------|
| `modify_property.lua` | Calls `Property.load()` which doesn't exist — crashes on invoke |
| `set_property.lua` | Calls `Property.create()` which doesn't exist — crashes on invoke |
| `debounce.lua` | Not required by any production module |
| `command_implementations.lua` | Removed modify_property and set_property from auto-load list |
| Real property command is `SetClipProperty` (fully tested) | |

## Dead Tests Removed (10 files)
| File | Reason |
|------|--------|
| `test_clip_window_cache_poisoning.lua` | Stub for removed C++ prefetch system |
| `test_clip_window_multitrack.lua` | Same |
| `test_null_check.lua` | Debug script, zero assertions |
| `test_null_check2.lua` | Same |
| `test_null_check3.lua` | Same |
| `test_null_check4.lua` | Same |
| `integration/test_clip_window_cache_poisoning.lua` | Integration stub |
| `integration/test_clip_window_debounce.lua` | Integration stub |
| `test_resolve_database_import.lua` | Exploratory doc, zero assertions |
| `test_modify_property_undo.lua` | Documented dead commands, then commands were deleted |

## New Tests (16 files, ~310 assertions)
| File | Checks | What it tests |
|------|--------|---------------|
| `test_load_clips_null_guards.lua` | 24 | Schema NOT NULL, load_clips API guards, integer roundtrip, timestamps, boundaries |
| `test_schema_sql_portability.lua` | 2 | RAISE() portability + schema execution (upgraded) |
| `test_table_utils.lua` | 31 | shallow_copy + deep_copy: non-tables, nested, circular, mutation independence |
| `test_cancel.lua` | 14 | request/consume/peek/clear lifecycle, signal emission |
| `test_browser_state_normalize_timeline.lua` | 23 | normalize_selection: integer duration, NTSC, nil, errors |
| `test_set_selection_command.lua` | 9 | Persist/overwrite/clear/nonexistent sequence |
| `test_set_viewport_command.lua` | 15 | Bounds, scroll offsets, split ratio, large values |
| `test_mark_clip_extent_command.lua` | 11 | Clip priority, boundaries, DRP-scale values |
| `test_sequence_save_load_roundtrip.lua` | 33 | All fields, timestamps, gap_infos, nil marks, DRP-scale |
| `test_load_sequences_duration.lua` | 18 | Duration from clips, empty seq, DRP-scale, error paths |
| `test_delete_sequence_undo_data_loss.lua` | 34 | All sequence/clip/link/property/track fields through delete+undo |
| `test_delete_clip_undo_preserves_metadata.lua` | 16 | Volume/marks/playhead survive delete+undo |
| `test_no_overlap_after_undo.lua` | 11 | No overlapping clips after delete+undo cycles |
| `test_frame_utils_edge_cases.lua` | 17 | fps=0 assert, standard/NTSC rates, parse roundtrip, negatives |
| `test_split_preserves_clip_metadata.lua` | 7 | Volume preserved on both halves of split |
| `test_command_state.lua` | (fixed) | Added project_id + owner_sequence_id to INSERT (caught by schema NOT NULL) |
| `test_overwrite_preserves_overwritten_volume.lua` | 6 | Overwrite undo volume loss (known issue: 5/6 pass, volume not restored) |
| `test_clip_delete_cleans_orphans.lua` | 14 | Clip:delete() removes properties + links for linked A/V clips |

## Existing Tests Fixed
| File | Fix |
|------|-----|
| `test_command_state.lua` | INSERT missing project_id (caught by NOT NULL constraint) |
| `test_database_errors.lua` | Removed test for deleted `update_clip_position` |

## Systems Audited (clean — no actionable bugs found)
| System | Notes |
|--------|-------|
| Renderer | Fail-fast architecture, offline frames handled, gaps clear display |
| Command history | Tree-based undo, branch cutoff tested, persistence correct |
| Database transactions | No leaks, proper WAL, single-threaded, SQL isolation |
| Gather context | Correct fail-fast asserts throughout (one source_in fallback in unintegrated code) |
| Timeline state cache | Refreshed via command_manager after every command |
| Trim/drag boundaries | Minimum duration, source bounds, negative position all protected |
| DRP blob parsing | Lua truthiness for 0 is correct (not a bug), bounds checks present |
| add_clips_to_sequence undo | Properties copied, cleaned up, mutations reverted correctly |

## Remaining Known Issues (not bugs — design gaps/debt)

### Feature gaps
- **Drop-frame timecode** unimplemented (NDF only currently)
- **Gap selection persistence** incomplete (in-memory only, not loaded/saved by timeline init)
- **FCP7 XML volume import** not implemented

### Lower-traffic undo data loss
- `lift_range.lua` / `extract_range.lua` — properties not captured through mutation path (uses resolve_occlusions which doesn't snapshot properties)
- `delete_master_clip.lua` — timeline clip clip_links not captured/restored on force-delete undo
- ~~Overwrite undo volume loss~~ **FIXED** by clone_state() root cause fix (commit 879c500). The "no mutations for Overwrite" warning is expected — Overwrite delegates to AddClipsToSequence via undo group. Test now passes: `test_overwrite_preserves_overwritten_volume.lua`.

### Code quality debt
- 140+ `print()` calls in 23 command files should use logger module
- Signals system silently logs handler errors (should assert per fail-fast policy)
- ~20 `LIMIT 1` queries without `ORDER BY` (deterministic by uniqueness but implicit)

### Test infrastructure
- `test_audio_mix_tmb.lua` SKIP_PUMP_TESTS — 300 lines skipped, should move to integration/
- ~10 single-assertion mock routing tests could be consolidated
- `test_browser_state_rational_duration.lua` — code-inspection test (kept; behavioral test added alongside)
