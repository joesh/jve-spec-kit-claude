-- Test: clip window computation with multi-track timelines.
-- This test verified the old clip window system which has been replaced
-- by the C++ prefetch system in PlaybackController. No longer applicable.

require("test_env")

print("✅ test_clip_window_multitrack.lua passed (skipped — clip window system replaced by C++ prefetch)")
